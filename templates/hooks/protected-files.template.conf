# Protected Files — Auto-generated by generate-protected-files.sh
# One pattern per line. Matches against both full path and filename.
# Lines starting with # are comments.
# Regenerate: bash .claude/hooks/generate-protected-files.sh
#
# These files are protected because modifying them without review
# can break the workflow engine, quality gates, or session protocols.

# Core infrastructure (always protected)
CLAUDE.md
_RULES.md
AGENT_DELEGATION.md
.git/hooks/
frameworks/
.claude/hooks/
.claude/settings.json

# Workflow scripts (universal)
db_queries.sh
session_briefing.sh
build_summarizer.sh
save_session.sh
work.sh
coherence_check.sh
coherence_registry.sh
milestone_check.sh

# From STOP rules in %%PROJECT_RULES_FILE%%

# Database files — fill per project
# %%PROJECT_DB%% — add your project's .db files here, one per line
# Example:
#   myproject.db
#   data.db

# Lessons file
%%LESSONS_FILE%%
