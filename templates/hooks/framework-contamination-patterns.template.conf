# Framework contamination patterns (case-insensitive grep -E)
# One pattern per line. Lines starting with # are comments.
#
# These are project-specific strings that should NOT appear in framework
# files (anything under .claude/frameworks/ or templates/frameworks/).
# Framework docs are meant to be reusable across projects — they must stay
# free of this project's names, paths, and identifiers.
#
# The bootstrap generator seeds %%PROJECT_NAME%% below as a starting point.
# Edit this file after bootstrap to add your project's DB filenames,
# domain jargon, and any other project-specific identifiers. Remember
# that patterns are regex — escape dots (e.g. `myproject\.db`) so they
# match literally rather than any character.

# Project identifier (seeded at bootstrap)
%%PROJECT_NAME%%

# Add project-specific patterns below. Examples:
#   myproject\.db
#   MapLibre
#   firmware
