# Framework contamination patterns (case-insensitive grep -E)
# One pattern per line. Lines starting with # are comments.
# These are project-specific strings that should NOT appear in frameworks.
Bootstrap
RomaniaBattles
MasterDashboard
Firmware
bootstrap\.db
master_dashboard
romania
