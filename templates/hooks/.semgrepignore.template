# .semgrepignore — primary exclusion mechanism for SAST scans
# Rule-level paths.exclude is unreliable with single-file scans (see Appendix B in SAST plan).
# Use this file for all directory/pattern exclusions.

# Vendored / generated
node_modules/
vendor/
__pycache__/
*.generated.*
*.min.js
dist/
build/
.git/

# Test fixtures (synthetic bad code for SAST testing)
tests/fixtures/sast/
